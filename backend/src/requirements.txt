# Runtime dependencies bundled into every Lambda by `sam build`. Pinned exactly
# (including transitive packages) so builds are reproducible; Dependabot proposes updates.
# boto3 is provided by the Lambda Python 3.12 runtime and is deliberately not bundled.
aws-lambda-powertools==3.35.0
jmespath==1.1.0
typing_extensions==4.16.0
